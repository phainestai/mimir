# Copy to infra/.env or repo-root .env (both are gitignored).
# Used by `cdk deploy` to set EB application secrets — not stored in eb_live_platform_settings.json.

DATABASE_URL=postgresql://user:password@host:5432/mimir
DJANGO_SECRET_KEY=change-me
ANTHROPIC_API_KEY=sk-ant-...
GITHUB_TOKEN=gho_...
